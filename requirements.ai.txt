# AI-Enhanced Genre Classification Requirements
# Cost-effective AI integration for music genre classification

# Core AI Dependencies
sentence-transformers==2.2.2     # FREE - Local embeddings, no API costs
numpy==1.24.3                    # Required for embeddings
scikit-learn==1.3.0             # For similarity calculations

# Optional API Dependencies (FREE TIERS AVAILABLE)
openai==1.3.7                    # OpenAI API - pay per use (~$0.0005/classification)
requests==2.31.0                 # For Hugging Face free API

# Existing Dependencies
sqlalchemy==2.0.23
psycopg2-binary==2.9.7

# Installation Priority:
# 1. Install sentence-transformers first (completely FREE, works offline)
# 2. For Hugging Face: Get free API key from https://huggingface.co/settings/tokens
# 3. For OpenAI: Only needed for highest accuracy cases (~$1 for 2000 classifications)

# Total Cost Breakdown:
# - Local embeddings: $0 (100% free)
# - Hugging Face free tier: $0 (rate limited)
# - OpenAI minimal usage: ~$0.50-$2 for full database
# - Caching prevents repeated API calls: 90%+ cost savings